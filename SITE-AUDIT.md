# ביקורת אתר מקיפה — ServeSite

תחקור עומק של כל הקוד, התשתית, האבטחה, הנגישות, הביצועים והעיצוב. **בדיוק 100 שיפורים דרושים**, מקובצים לפי תחום, כל אחד עם הפניה לקובץ הרלוונטי.

---

## אבטחה (1–25)

1. **אין כותרת Content-Security-Policy** — `next.config.ts` מגדיר כותרות אבטחה אך לא CSP; הוספת CSP היא ההגנה החזקה ביותר נגד XSS באתרי לקוחות שמציגים תוכן שמשתמשים הזינו.
2. **הזרקת JSON-LD ללא escaping** — `app/site/[businessSlug]/page.tsx:54` משתמש ב-`dangerouslySetInnerHTML` עם `JSON.stringify` בלבד. בעל עסק זדוני ששומר `</script><script>...` בשם/תיאור העסק מזריק סקריפט לכל מבקר. יש להחליף כל `<` ברצף המקודד `\u003c` לפני ההזרקה.
3. **קישורי רשתות חברתיות בלי ולידציה** — `components/public-website.tsx:472` מרנדר `href={url}` ישירות; אפשר לשמור `javascript:` URL בהגדרות ולהריץ קוד אצל מבקרים. לאמת שהכתובת מתחילה ב-`http(s)://` בשמירה וברינדור.
4. **`mapUrl` בלי ולידציה** — אותה בעיה ב-`public-website.tsx:404` עבור קישור "Open in maps".
5. **אין ולידציה צד-שרת ל-URL של תמונות** — heroImage, לוגו ותמונות תוכן נשמרים ומרונדרים כ-`src` בלי בדיקת סכימה (data:/blob: וכד').
6. **אין rate limiting על `/api/track`** — `app/api/track/route.ts` מאפשר לכל אחד לנפח מוני צפיות בלולאה פשוטה.
7. **`record_site_visit` נגיש ישירות דרך PostgREST** — הפונקציה ניתנת ל-anon; אפשר לקרוא לה ישירות ב-RPC של Supabase ולעקוף אפילו את סינון הבוטים של ה-API route. נדרשת הגנת קצב בצד הדאטהבייס.
8. **אין הגנה אמיתית מספאם בטופס הלידים** — ה-honeypot ובדיקת התזמון ב-`quote-request-form.tsx:81` הם client-side בלבד; בוט יכול להכניס לידים ישירות דרך Supabase REST עם ה-anon key. נדרש CAPTCHA/turnstile או Edge Function עם ולידציה.
9. **אין מגבלות אורך בדאטהבייס לשדות לידים** — אפשר להכניס מגה-בייטים לכל שדה טקסט; להוסיף CHECK constraints לאורכים.
10. **אין ולידציית פורמט אימייל/טלפון ברמת הדאטהבייס** — הולידציה של zod היא client-side בלבד.
11. **סיסמה מינימלית 6 תווים** — `app/signup/page.tsx:27`; להעלות ל-8+ ולהפעיל בדיקת סיסמאות שדלפו (HIBP) ב-Supabase Auth.
12. **אין אימות דו-שלבי (MFA)** — Supabase תומך TOTP; חשבון מנהל אתר עסקי מצדיק זאת.
13. **אין תהליך איפוס סיסמה** — "Forgot password" מציג `window.alert` בלבד (`app/login/page.tsx:55`); לממש `resetPasswordForEmail`.
14. **"Remember me" מזויף** — checkbox בלוגין שלא מחובר לכלום; להסיר או לממש.
15. **הודעות שגיאה גולמיות של Supabase מוצגות למשתמש** — `signInError.message` ודומיו חושפים פרטים פנימיים; למפות להודעות ידידותיות.
16. **`requestOrigin` סומך על כותרת Host** — `lib/seo.ts` בונה canonical/sitemap/OG מכותרת הבקשה; חשוף ל-host header poisoning. לקבע דומיין קנוני במשתנה סביבה.
17. **אין מכסה או rate limit על `/api/uploads`** — משתמש מחובר יכול להעלות אינסוף תמונות ולנפח עלויות Blob.
18. **תמונות שהוחלפו לא נמחקות מ-Vercel Blob** — קבצים יתומים נשארים ציבוריים לנצח; גם בעיית פרטיות (תמונה "שנמחקה" עדיין נגישה ב-URL).
19. **HSTS בלי `preload`** — `next.config.ts:14`.
20. **חסרות כותרות COOP/CORP** — `Cross-Origin-Opener-Policy` ו-`Cross-Origin-Resource-Policy`.
21. **אין ולידציית slug צד-שרת** — ההגדרות מנקות slug רק client-side (`settings/page.tsx:176`); slug ריק או ערך שמור ("api", "www") אפשריים דרך קריאה ישירה.
22. **קוד מת שסומך על כותרות זהות** — `app/chatgpt-auth.ts` קורא כותרות `oai-authenticated-user-*` הניתנות לזיוף; הקובץ לא בשימוש — למחוק לפני שמישהו יחבר אותו בטעות.
23. **אין ניטור אירועי אבטחה** — אין לוג נסיונות התחברות כושלים ואין התראות.
24. **אין מחיקת חשבון ונתונים** — נאסף PII של לקוחות קצה (לידים) ללא כל מנגנון מחיקה — חשיפה רגולטורית (GDPR).
25. **אין מדיניות פרטיות ותנאי שימוש אמיתיים** — הטפסים אוספים PII והטקסט "demo Terms and Privacy Policy" לא מקשר לשום דף.

## ביצועים (26–40)

26. **שמירה ל-Supabase על כל הקשת מקלדת** — `persistBusiness` רץ בכל `onChange` בהגדרות (`settings/page.tsx:63-72`); הקלדת משפט אחד = עשרות בקשות. להוסיף debounce.
27. **Race condition בשמירות רצופות** — תשובות עלולות לחזור שלא בסדר ולדרוס עדכון חדש בישן; אין ביטול בקשות קודמות (AbortController).
28. **הפונטים הנבחרים לא נטענים בכלל** — אין `@font-face` או import ל-Cormorant Garamond / Playfair Display / Fraunces; כל בחירת טיפוגרפיה נופלת בשקט ל-fallback מערכת. באג מרכזי בליבת מוצר העיצוב.
29. **אתר הלקוח כולו client component אחד** — `public-website.tsx` (521 שורות) נשלח כ-JS מלא לכל מבקר; לפצל לרינדור שרת עם איים אינטראקטיביים קטנים.
30. **אין `sizes` על אף תמונה** — הדפדפן מוריד רזולוציה מלאה גם במסכים קטנים.
31. **`sizedImage` תומך רק ב-unsplash** — תמונות שהועלו ל-Vercel Blob לא מוקטנות כלל (`lib/utils.ts:65-78`).
32. **אין preconnect ל-host של Vercel Blob** — רק ל-unsplash (`app/layout.tsx:47`).
33. **אין caching/ISR לאתרי לקוחות** — כל בקשה מריצה 11 שאילתות Supabase (`get-public-business.ts:30-47`).
34. **11 שאילתות במקום אחת** — לאחד ל-view או RPC שמחזירה bundle שלם.
35. **`decoding="sync"` על תמונות priority** — חוסם את ה-main thread; `async` עדיף גם ל-LCP (`site-image.tsx:62`, `app/page.tsx:64`).
36. **אנימציית reveal-on-scroll בלי fallback** — `animation-timeline: view()` (`globals.css:1309`) לא נתמך בפיירפוקס/ספארי; להוסיף IntersectionObserver.
37. **אין פיצול קוד לרכיבים כבדים** — lightbox, טופס הצעת מחיר וטפסי עריכה יכולים להיטען ב-dynamic import.
38. **אין מדידת Web Vitals** — אין ניטור ביצועים אמיתי מהשטח.
39. **ה-sitemap שולף עד 5000 עסקים בבת אחת** — בלי עימוד ובלי sitemap index; יקרוס בקנה מידה (`app/sitemap.ts`).
40. **נכסים סטטיים בלי אופטימיזציה** — og.png ותמונות השיווק לא עוברים דרך next/image או דחיסה מוקדמת.

## נגישות (41–52)

41. **ConfirmDialog בלי מלכודת פוקוס, Escape והחזרת פוקוס** — `components/ui.tsx:48-65` לא משתמש ב-`useModalBehavior` הקיים; מחיקת פריט אינה נגישה למקלדת כראוי.
42. **skip-link באתר הציבורי מדלג לטופס** — "Skip to the inquiry form" (`public-website.tsx:444`) במקום לתוכן הראשי.
43. **`lang="en"` קבוע גם לאתרי לקוחות** — אין הגדרת שפה וכיוון (עברית/RTL) פר-אתר; קריטי לקהל יעד ישראלי.
44. **שגיאות התחברות/הרשמה/אונבורדינג בלי `role="alert"`** — קוראי מסך לא מכריזים עליהן (`login/page.tsx:52`, `signup/page.tsx:63`, `onboarding/page.tsx:152`).
45. **hint של Field לא מקושר לשדה** — אין `aria-describedby` (`ui.tsx:44-46`).
46. **אין בדיקת ניגודיות לצבעי הלקוח** — בחירת פלטה חופשית יכולה לייצר אתר לא קריא; להוסיף אזהרת ניגודיות WCAG בעורך העיצוב.
47. **drawer הניווט במובייל בלי מלכודת פוקוס** — רק Escape מטופל (`public-website.tsx:117-127`).
48. **כפילות הכרזה בכרטיסי הגלריה** — גם `aria-label` על הכפתור וגם `alt` על התמונה; ולתמונה בלי caption ה-label הוא "Open " ריק (`public-website.tsx:318`).
49. **מונה ה-lightbox לא מוכרז** — "1 of 6" משתנה בלי `aria-live` (`public-website.tsx:516`).
50. **טוסט נעלם אחרי 2.8 שניות קבועות** — קצר מדי, והטיימר הקודם לא מבוטל כשמגיע טוסט חדש כך שהודעות נחתכות (`app-provider.tsx:29-32`).
51. **mini-chart כ-`role="img"` עם aria-label ארוך** — עדיף טבלת נתונים חלופית לקוראי מסך (`dashboard/page.tsx:146`).
52. **ניווט ההגדרות בלי `aria-current`** — הטאב הפעיל מסומן ויזואלית בלבד (`settings/page.tsx:85-92`).

## עיצוב ו-UX (53–80)

53. **כפתור "Save changes" מזויף בהגדרות** — רק מציג טוסט "Settings saved" (`settings/page.tsx:83`); השמירה האמיתית קרתה כבר בכל הקשה. מטעה — לבחור מודל שמירה אחד.
54. **שגיאת שמירה מוצגת כטוסט הצלחה** — `notify("Couldn't save…")` מוצג עם אייקון ✓ ירוק; אין וריאנט שגיאה לטוסט (`app-provider.tsx:40-44`).
55. **עדכונים אופטימיים בלי rollback** — חוץ מ-publish, כשל שמירה משאיר את ה-UI מציג נתונים שלא נשמרו בפועל.
56. **`window.alert` לאיפוס סיסמה** — חוויה שבורה בדף הכניסה.
57. **אין חיווי "שומר… / נשמר"** — בעורכי התוכן אין שום אינדיקציה שהשינוי הגיע לשרת.
58. **אין אישור לפני הסתרת אתר חי** — כיבוי ה-toggle מוריד את האתר מיידית בלי אזהרה (`settings/page.tsx:171`).
59. **האונבורדינג מפרסם אוטומטית** — העסק נוצר עם `published: true` בלי לשאול (`onboarding/page.tsx:92`).
60. **publish לא אטומי** — אם הוספת sections נכשלת נשאר עסק יתום, ולחיצה חוזרת יוצרת עסק כפול; לעטוף ב-RPC אחת (`onboarding/page.tsx:89-137`).
61. **טיוטת אונבורדינג לא נשמרת** — refresh מאבד את כל חמשת השלבים.
62. **שמות פונטים לא עקביים בין אונבורדינג לעיצוב** — האונבורדינג שומר "Cormorant"/"Playfair"/"Source Sans" (`onboarding:174`) בעוד עמוד העיצוב מציע "Cormorant Garamond"/"Playfair Display" (`design/page.tsx:16`) — הבחירה לא תתאים לרשימה בדשבורד.
63. **ברירות מחדל בעייתיות לפריט תפריט חדש** — תמונת unsplash זרה, תווית "Kosher", "72 hours", "10 guests" נכנסים אוטומטית לכל מנה חדשה (`menu/page.tsx:16-35`).
64. **טקסטים קשיחים באתר הלקוח** — "Discover our table", "Every menu can be tailored…", "Every gathering tells its own story." אינם ניתנים לעריכה (`public-website.tsx:169,295,306`).
65. **הבטחת זמן תגובה קשיחה בשם העסק** — "Response time: Within one business day" מופיעה ליד הטופס ובהודעת ההצלחה בלי שהעסק בחר בכך (`public-website.tsx:422`, `quote-request-form.tsx:123`).
66. **ה-lightbox מוגבל ל-galleryLimit** — גם בסינון "All work" מוצגות רק התמונות עד המגבלה; המבקר לא רואה את כל הגלריה.
67. **אין גרירה לסידור מחדש** — סידור גלריה/תפריט/סקשנים רק בחצים; drag & drop מתבקש.
68. **אין סינון לידים לפי טווח תאריכים/חיפוש חופשי מתקדם** בטבלת הלידים.
69. **אין ייצוא לידים ל-CSV** — נתוני ה-CRM כלואים במערכת.
70. **התצוגה המקדימה אינה iframe** — ה-preview מרונדר באותו DOM עם ה-toolbar; CSS גלובלי עלול לדלוף בין הדשבורד לאתר.
71. **אין מצב כהה לדשבורד** — `colorScheme: "light"` קבוע (`app/layout.tsx:17`).
72. **הדומיין המוצג פיקטיבי** — "slug.servesite.co" מוצג כ"כתובת שלך" אבל האתר חי רק ב-`/site/slug`; מצג שווא ללקוח (`dashboard/page.tsx:27`, `settings:175`).
73. **קישור הדמו עלול להישבר** — דף השיווק מקשר ל-`/site/olive-and-ember` שיחזיר 404 אם עסק הדמו לא קיים בסביבה (`app/page.tsx:56,113,124`).
74. **"© 2026" קשיח בפוטר השיווקי** — `app/page.tsx:124`.
75. **"Explore feature" נראה כקישור אך אינו לחיץ** — כרטיסי הפיצ'רים בדף הבית (`app/page.tsx:80`).
76. **עמוד "האתר לא זמין" לא ממיר** — מפנה רק ל-ServeSite; אין למבקר שום דרך להשאיר פרטים או להבין מתי האתר יחזור (`site/[businessSlug]/not-found.tsx`).
77. **אין תמיכה רב-לשונית** — כל המוצר באנגלית בלבד למרות תמחור בשקלים וקהל יעד ישראלי; נדרש i18n מלא כולל RTL.
78. **אין favicon/OG מותאמים לאתר הלקוח** — הלוגו של העסק לא הופך ל-favicon ולתמונת שיתוף ממותגת.
79. **הגדרת "Email for new leads" לא מחוברת לכלום** — אין שליחת אימייל בפועל על ליד חדש; הפיצ'ר החשוב ביותר לבעל עסק חסר.
80. **ריבוי פיצ'רים מדומים בלי תיוג עקבי** — "Create quote", חיבור דומיין, החלפת תוכנית — חלקם מסומנים "Coming soon" וחלקם רק מציגים טוסט; לאחד שפה ויזואלית ל"בקרוב".

## SEO (81–88)

81. **meta description עלול להיות ריק** — כשאין tagline ואין description אין fallback (`site/[businessSlug]/page.tsx:15`).
82. **חסרים `og:locale` ומידות לתמונת השיתוף** — לאתרי לקוחות אין width/height ל-og:image.
83. **aggregateRating מהמלצות שהבעלים מזין בעצמו** — מפר את הנחיות Google בנושא self-serving reviews ועלול לגרור אובדן rich results (`lib/structured-data.ts:48-57`).
84. **lastModified בסייטמאפ לא משקף שינויי תוכן** — רק `updated_at` של רשומת העסק; עדכון תפריט/גלריה לא מעדכן אותו (`app/sitemap.ts:24`).
85. **אין הבטחת H1 יחיד** — אם סקשן ה-hero מוסתר אין H1 בדף בכלל; ה-H1 תלוי בטקסט חופשי של המשתמש.
86. **אין דפי גילוי ציבוריים** — רשימת קייטרינג לפי עיר/קטגוריה תחזק את ה-SEO של הפלטפורמה ותביא תנועה ללקוחות.
87. **אין structured data לדף השיווק** — חסר `Organization`/`WebSite`/`SoftwareApplication` ב-`app/page.tsx`.
88. **אין אכיפת דומיין קנוני** — אין redirect בין www/non-www ואין נירמול trailing slash.

## אמינות ונתונים (89–95)

89. **אי-התאמת אזורי זמן באנליטיקס** — הצפיות נרשמות לפי יום UTC (`migration:44`) אבל הדשבורד מקבץ לפי יום מקומי (`analytics.ts:5-8`); בישראל צפיות ערב נספרות ליום הלא נכון.
90. **צפייה אובדת בכשל רשת** — `sessionStorage` מסומן לפני שה-fetch הצליח (`visit-tracker.tsx:13-14`), וב-StrictMode בפיתוח ה-abort + הדגל מונעים רישום בכלל.
91. **סינון בוטים לפי user-agent בלבד** — לא תופס בוטים מודרניים שמזייפים UA (`api/track/route.ts:4`).
92. **image-uploader מניח תשובת JSON** — `response.json()` יזרוק חריגה על תשובת 502 HTML מ-proxy (`image-uploader.tsx:33`).
93. **אין retry/טיפול offline** — כשל רשת בשמירות הדשבורד מציג טוסט ומאבד את השינוי.
94. **פורמט תאריכים קשיח ל-en-US** — `formatDate`/`relativeTime`/גרף הימים לא מכבדים locale (`lib/utils.ts:23,29,37`).
95. **ערכי סנטינל מזהמים את הדאטהבייס** — "Not specified", "None shared", "Open to recommendations" נשמרים כטקסט במקום NULL, ומקשים על סינון ודוחות (`quote-request-form.tsx:94-104`).

## איכות קוד ותשתית (96–100)

96. **אין CI** — אין GitHub Actions שמריץ lint/typecheck/tests על כל push.
97. **כיסוי בדיקות חלקי** — אין בדיקות ל-mappers, ל-`/api/uploads`, ל-`/api/track` ולרכיבי הטפסים.
98. **שאריות קוד לא בשימוש** — `.openai/hosting.json` ו-`app/chatgpt-auth.ts` אינם מחוברים לכלום; למחוק.
99. **אין דיווח שגיאות (error tracking)** — `global-error.tsx` בולע שגיאות בלי לשלוח ל-Sentry או מקבילה.
100. **סכימת הדאטהבייס לא מתועדת בריפו** — קיימת רק מיגרציה אחת חלקית; ליישר את כל הסכימה כסדרת מיגרציות מלאה בגרסאות כדי שאפשר יהיה לשחזר סביבה מאפס.
